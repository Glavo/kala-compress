module kala.compress {
    requires transitive kala.compress.base;
    requires transitive kala.compress.changes;
    requires transitive kala.compress.archivers;
    requires transitive kala.compress.compressors;
}